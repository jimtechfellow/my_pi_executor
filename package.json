{
  "name": "executor-runtime",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "Task-scoped Pi runtime bundled with the canonical Executor Skill.",
  "scripts": {
    "check": "node --check executor/scripts/runtime.mjs && node --check executor/scripts/pi_rpc.mjs && node --check executor/scripts/mission_store.mjs && node --test tests/architecture.test.mjs"
  }
}
